version: '3.8'

services:
  morsemeplease:
    build: .
    container_name: morsemeplease
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    networks:
      - morsemeplease-network

networks:
  morsemeplease-network:
    driver: bridge
